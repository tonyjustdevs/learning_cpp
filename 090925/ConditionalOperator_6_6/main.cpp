#include<iostream>

int main() {

    int x{ 2 };
    //std::cout << (x < 0) ? "negative" : "non-negative";
    //std::cout << false ? "negative" : "non-negative";
    // 0 is printed, std::cout is returned
    //std::cout ? "negative" : "non-negative";
    return 0;
}